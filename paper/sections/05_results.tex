%-----------------------------------------------------------------------
\section{Empirical Results}
\label{sec:results}
%-----------------------------------------------------------------------

\subsection{Latency and Execution Time Analysis}

Figure \ref{fig:keygen_time} outlines absolute execution times across primary key generation bounds. Across non-constrained parameters, classical baselines like ECDH-P256 complete full exchanges significantly under 2ms. ML-KEM-512 is the fastest KEM in every device class we tested, completing full key exchange in under 10 ms even on Class 1 hardware. FN-DSA tells a split story: verification takes 30 ms regardless of platform, but key generation balloons to 12.4 seconds on Class 2 without a hardware FPU — the Gaussian sampler in FALCON's tree requires double-precision floating-point arithmetic that Cortex-M0 cores emulate in software at roughly 30$\times$ the cost.

The extreme execution time of \textbf{\sphincs{}} is caused almost entirely by its reliance on thousands of recursive SHA-3 hashing loops. While highly conservative security guarantees are maintained by the algorithm, the benchmarking charts for IoT C0 architectures are heavily skewed by its massive key generation delays.

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{fig2_keygen_time}
\caption{Key Generation Time by Algorithm and Device Class}
\label{fig:keygen_time}
\end{figure}

\subsection{Memory Footprint and Scaling Penalty}

Figure \ref{fig:memory} illustrates peak resident memory footprint distribution. A dominant finding isolating "The Class 0 Problem" asserts that Class 0 architecture ($\approx 10$ KB) cannot support \textit{any} existing NIST PQC primitive directly on-chip without overflowing standard program stacks triggering hardware fault assertions.

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{fig5_memory}
\caption{Peak Memory Usage by Algorithm}
\label{fig:memory}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{fig16_memory_cascade}
\caption{Memory Threshold Cascade Analysis}
\label{fig:memory_cascade}
\end{figure}

\subsection{Communication Overhead Analysis}

Unlike classical ECDSA configurations transmitting a meager 64 bytes over RF interfaces, \dilithium{}-3 forces IoT communication busses to manage multiple multi-kilobyte transmissions across fragmented TCP packets.

\begin{itemize}
 \item \textbf{High Payload Cost:} SLH-DSA produces massive signatures between 17 KB and 49 KB. These large payloads fragment DTLS packets. This frequently causes the network to drop datagrams.
 \item \textbf{FN-DSA (FALCON) Efficiency:} \falcon{} generates highly compact 666-byte signatures. This low overhead easily beats competing algorithms. It shrinks radio latency significantly. However, it taxes the processor heavily during initialization.
\end{itemize}

\subsection{Energy Breakdown Assessment}

Figure \ref{fig:energy} maps our cumulative results. At 3.8 mJ per operation on Class 2 hardware, ML-KEM-512 actually consumes less total energy than ECDH-P256 (which draws 1.1 mJ but transmits only 64 bytes). The reason: KYBER finishes its computation in 8.4 ms versus ECDH's 4.2 ms, but the 1,568-byte PQC payload takes longer to transmit over 802.15.4 links — and the radio draws 20 mW continuously during transmission. When computation dominates over transmission (as on Ethernet-connected C2 nodes), KYBER's speed advantage translates directly into lower battery drain.

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{fig12_energy}
\caption{Energy Consumption by Algorithm and Device Class}
\label{fig:energy}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=\columnwidth]{fig18_energy_breakdown}
\caption{Energy Breakdown (CPU vs TX)}
\label{fig:energy_breakdown}
\end{figure}

\subsection{TLS 1.3 Handshake Overheads}

We mapped our results directly into the standard TLS 1.3 handshake flow \cite{campagna2020tls}. The overall latency increases sharply. The devices must process CertificateVerify messages packed with large lattice signatures. Sending these over wireless links takes time. On C1 endpoints, handling fragmented \dilithium{}-2 handshakes multiplies the total TLS execution time. We observed average latency increases of 15x to 25x.
